> Node.js, Bun, JS runtime & npm.
> CLI that lets the user specify a file path and 
    the nodejs process counts the number of words 
    inside it.
> HTTP
    - IP, ports, methods, response, status code, body & 
    routes.
    - postman.
> note.txt file has some wsl commands
