// to show loader to show before fallback UI
// reccommand to not use it in landing page becz it returns loading to the google crawlers
export default function Loader() {
    return <div>
        Loading...
    </div>
}